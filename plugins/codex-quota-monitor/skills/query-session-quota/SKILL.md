---
name: query-session-quota
description: Open the local Codex quota monitor or explain its session quota estimates, refresh costs, burn rate, and reset countdown.
---

Use `open_quota_monitor` to get the local dashboard URL. Read the exact `url` returned by the tool; when `open_in_codex` is available, open that exact URL in a Codex browser side panel. Whether or not the side panel opens, the final reply must include the same exact URL as a clickable Markdown link, using `[打开 Codex 额度监控器](<EXACT_URL>)`. Use the product name directly; do not prefix normal monitor links with “真实” or “真实账户”. Do not reconstruct, shorten, redact, or replace the port/token-bearing URL with a generic localhost link. Default to the full dashboard (`compact: false`) unless the user asks for a compact layout. After opening, verify the page displays dashboard content rather than JSON errors or only a connection strip when browser tools are available; otherwise report only the asset/API checks actually performed. Opening does not wait for upstream quota queries. For a one-off answer, `get_quota_snapshot` reads the cached snapshot. Use `diagnose_quota_monitor` for startup or stale-data errors.

The saved loopback endpoint keeps its port/token on the same machine, so a bookmark works while that monitor service is running. A bookmark cannot start a stopped service and cannot automatically open the ChatGPT desktop app; tell the user to run/open the monitor tool again when the service is stopped. A Codex side-panel open is not proof that the native desktop window was opened.

Keep these distinctions visible:
- The top monitoring-source selector switches between the Codex account and providers configured in the desktop app, including Muse and other custom APIs. It does not change the provider of a running task or write provider credentials. The initial monitoring view follows the detected desktop provider; later explicit selections persist.
- API provider views report recorded token usage, never Codex subscription percentages, countdowns, or Codex credit prices. Their execution charts show token consumption and recorded duration, without a time-per-1% metric. Official OpenAI API USD reference estimates can be shown separately; actual provider charges and balances remain unknown without provider records. Ambiguous mixed-provider history remains separate; only continuously observed subsequent increments can be assigned to the observed provider with partial coverage. Each API task is counted independently, without adding child usage into its parent again; visual parent grouping does not change these totals. Unclassified tokens are included in totals and do not imply a known input/output split.
- API charts group usage samples from one logical turn into one execution; incremental token samples must not become separate executions. Use original lifecycle start/end for whole-turn duration, including tool waits and intervals without token deltas. Token amounts retain their own coverage and provider provenance. A turn spanning providers can appear once in each applicable view with that provider's known tokens and the same whole-turn duration; never label this provider-exclusive runtime. If lifecycle timings are missing, show recorded observation duration marked partial.
- Muse summary, task, and turn-tooltip costs use the short-context baseline from [official OpenAI API pricing](https://developers.openai.com/api/docs/pricing), checked 2026-09-20. Price attributable per-increment model, tier, and token groups; never apply the latest task model to all mixed history. Use known supported Fast/Priority/Flex rates; a missing tier assumes Standard and must be disclosed. Unknown models, unsupported tiers, and unclassified tokens remain unpriced. Display priced coverage and unpriced token counts; partial USD amounts cover only the priced portion. Cached tokens are a subset of input, and reasoning tokens are a subset of output; count each once.
- This USD reference is separate from Codex credits and subscription percentages, and is not a Muse bill or balance. Cache-write counts and per-request context lengths are unavailable: treat non-cached input at the ordinary input rate and disclose that the baseline excludes cache-write uplift, long-context premiums, tool/region fees, and provider markup.
- Account windows come from `account/rateLimits/read`; show their sample time and error state. The reset panel separates 本地重置 (current account countdown and depletion estimate) from 官方通知 (official global announcements). Third-party forecasts are a separate, explicitly unofficial reference disclosure.
- Task percentages prefer recorded per-turn model, input/cache/output token breakdowns and published Codex credit weights, calibrated against same-account quota changes. Published weights are not official subscription percentages. Legacy equal-token records that cannot be recalibrated remain explicitly marked; other-device usage and historical pricing can affect accuracy. Show task consumption with two decimals by default, four below 0.01%, six below 0.0001%, continuing in pairs so positive values never render as zero; use scientific notation for extremely small values. Keep full calculation precision.
- Task totals and execution durations cover all known locally readable or previously recorded task history and persist independently of the account statistics window. Label the Codex inline metrics “总消耗额度” and “平均每1%耗时”; show execution duration and per-run details in the segmented chart, and disclose that unrecorded or unreadable history cannot be recovered. Average duration per percent divides known total duration by known estimated usage; identify it as a rough reference when their coverage differs. Account trends, attribution, and current calibration use the configured sliding window (24 hours by default, adjustable from 1 to 168 hours). The attribution panel skips incomplete history and computes from subsequent complete quota samples, displaying its effective start time; task estimates can still include retained historical observations. If no quota change has been observed yet, show a waiting state instead of a fabricated ratio. Increasing the window cannot recover observations already removed. Retain valid same-account consumption samples across quota resets inside the window. The remaining-quota chart shows retained official samples, while cumulative consumption uses observed changes; changing the attribution scope does not redefine these trend samples. Do not invent observations across missing or damaged history.
- Keep task totals separate from individual execution figures. Codex parent quota totals include discovered children; parent elapsed time merges concurrent intervals without counting them twice, and parent charts include discovered child executions. Keep parents above descendants for every ordering. Default to the newest lifecycle activity in each group, including descendants; users can select total usage or mean consumption speed ascending/descending, or drag handles to reorder siblings. Moving a parent carries its descendants and does not change parentage. Store view ordering per provider in the browser; missing sort values stay last. Never label task totals as limited to the account window. Missing cached task metadata must not keep a formerly active timer running. Never substitute cumulative quota for a missing execution estimate. Prefer available per-turn token-cost reconstruction for missing usage; otherwise show a waiting/no-record state rather than a fabricated zero.
- When the backend tags a completed-turn token recovery, explain that the value is calibrated from recorded token metadata rather than a directly allocated account charge. This recovery stays separate from confirmed account attribution. Do not invent a recovery from elapsed time or a multi-turn lifetime token total.
- Execution tooltips use that execution's recorded elapsed time, amount, start/end, and mean time per percent. The mean is elapsed time divided by the execution's estimated quota, not a prediction for the next turn. When duration or a positive quota denominator is unavailable, show the appropriate waiting/no-record state. Do not substitute a task-wide average or a future forecast for that execution's own figures.
- Format processing and rate durations with integer hours, minutes, and seconds, for example “1时2分3秒”, “11分57秒”, or “48秒”; omit unused leading units and do not imply sub-second precision.
- One shared refresh interval controls local tasks, account reads and the page: default 5 seconds, range 5–3600 seconds. Local dashboard refreshes make no model calls; the conversation used to open or explain the monitor still consumes normal model usage.
- Only describe native inline status and native window callbacks as supported if the capability flags say so. This version uses a separate dashboard.
- The attribution panel switches between retained history and retained observations from the current quota period. An unknown period start, late monitoring start, or expired samples means current-period coverage may be partial; do not present it as a complete period bill. The account chart defaults to remaining quota in a 0–100% range and rises on resets; its alternate cumulative mode can exceed 100%. Hover/focus points for time and quota; select a consumption sample to expand descending task shares totaling 100%, including unattributed usage. Preserve the selected sample and its expanded/collapsed state across refreshes. A sample can represent more than 1%, so disclose its actual delta instead of fabricating separate 1% bills. Per-task charts default to a collapsed preview beside the task, with independent expand/collapse choices retained per provider and task in the browser. Their X axis is cumulative task running time: merge concurrent execution spans and remove idle gaps between executions. Keep the original earliest-start and latest-end wall-clock endpoint labels, and exact wall-clock times in tooltips. Color executions separately on this compressed axis. Intermediate progress based only on start/end and totals is labeled as estimated.
- The theoretical usage check prices complete execution records using input, cached input, and output tokens. Cached input is part of total input; reasoning output is already included in output. Credits are not subscription percentages. A separate retrospective check calibrates the earlier matched account samples and evaluates later non-overlapping samples; show both scopes and differences, and wait when independent samples are unavailable.
- Model overview colors identify recent own-turn means, recent local estimates, or missing quota samples. Only attribute a rate to its identified execution model and effort; never relabel a mixed task history with the latest task setting or convert Radar cost/hour ratios into subscription quota rates. Unmeasured efforts retain external references with no quota prediction. Hourly consumption need not rise with effort because workload, caching, output, and tool waits differ. Depletion estimates use observed account consumption over at most the last 24 hours, report actual coverage, and exclude quota top-ups from consumption; official reset cards display the latest official timestamp, sample time, and a ticking seconds countdown.
- Model automation requires the user's explicit dashboard choice and affects future defaults. It does not switch an active turn or submit a prompt.

Never resolve monitor failures by patching the Codex application, editing its conversation databases, resuming existing threads in the collector, or sending empty turns. Keep monitoring separate from user work.
